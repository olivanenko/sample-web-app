pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/olivanenko/sample-web-app.git'
            }
        }
        stage('Build'){
           steps {
                echo "Build"
            }
        }
        stage('Test'){
           steps {
                echo "Test"
            }
        }
    }
}
